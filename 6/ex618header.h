#include <vector>
#ifndef COMPARE
#define COMPARE 
bool compare(const matrix& mx);
#endif
#ifndef CHANGE_VAL
#define CHANGE_VAL
std::vector<int> change_val(int i, std::vector<int>::iterator iter);
#endif:

