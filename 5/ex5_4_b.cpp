# include<iostream>
# include<string>
#include<vector>
using std::string; 
using std::cout;
using std::endl;
using std::vector;
int main() {
//while (bool status = find(word)) { /* . . . */ } if (!status) { /* . . . */ }
string s = "nozomi takemura is from japan word";
bool status;
char word = 'j';
it = vector::iterator
string::iterator iter = s.begin();
if (find(s.begin(), s.end(), word) != s.end())
	std::cout << word << " exists!" << std::endl;
	status = True;
else
	std::cout << word << " does not exist..." << std::endl;		
	status = False;
}
