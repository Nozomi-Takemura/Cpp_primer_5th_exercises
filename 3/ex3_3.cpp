/*
String input such as; std::cin >> string, hadles the white space as a stopping criteria. This means that std::cin >> string will read from stream until a white space appears. On the other hands, getline() function ignore a white space, and keep reading from stream untill newline is found.
*/
